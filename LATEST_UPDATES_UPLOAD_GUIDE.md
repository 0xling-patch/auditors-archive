# 最新動態上傳指南

「最新動態」頁面會在每次網站建置時，自動讀取 GitHub 儲存庫中的媒體檔案。你只需要上傳檔案並提交，不需要再修改程式、填寫資料表或撰寫額外文案。

## 上傳位置

請在同一個 GitHub 儲存庫的以下資料夾新增檔案：

```text
public/updates/
```

可直接開啟：[GitHub 最新動態資料夾](https://github.com/0xling-patch/auditors-archive/tree/main/public/updates)

## 檔名規則

> **檔名就是動態文案。** 系統會依檔名開頭的日期與時間，由新到舊自動排列。

請使用以下固定格式：

```text
YYYY-MM-DD_HHMM_你的文案.副檔名
```

| 上傳內容 | 建議檔名 | 頁面顯示文案 |
| --- | --- | --- |
| 圖片 | `2026-08-27_1430_雨後的監測站.webp` | 雨後的監測站 |
| 影片 | `2026-08-27_2215_深夜的終端機紀錄.mp4` | 深夜的終端機紀錄 |
| 同日較早內容 | `2026-08-27_0930_上午訊號測試.jpg` | 上午訊號測試 |

日期與時間請使用 **UTC**；例如台灣時間晚上 22:15，檔名應使用同日 14:15 UTC。較新的日期／時間會顯示在最上方。

## 支援格式與建議

| 類型 | 支援格式 | 建議 |
| --- | --- | --- |
| 圖片 | `.jpg`、`.jpeg`、`.png`、`.webp`、`.avif`、`.gif` | 優先使用 `.webp` 或 `.jpg`，縮小讀取時間。 |
| 影片 | `.mp4`、`.webm`、`.mov`、`.m4v` | 優先使用 H.264 `.mp4`，並先壓縮至適合手機播放的大小。 |

請盡量使用適合網頁的檔案大小。影片會在讀者按下播放後才開始傳輸完整內容；圖片則會在滑至接近卡片時延遲載入。

## GitHub 網頁上傳步驟

1. 開啟 [public/updates 資料夾](https://github.com/0xling-patch/auditors-archive/tree/main/public/updates)。
2. 選擇 **Add file**，再選擇 **Upload files**。
3. 將檔名改為上述格式後拖入圖片或影片。
4. 在頁面底部填寫提交訊息，例如 `post: add August field update`，然後按 **Commit changes**。
5. Cloudflare Pages 會收到提交並重新建置。稍候開啟網站導覽列的 **UPDATES／動態**，即可看到新內容。

## 注意事項

檔案若沒有以日期、時間與文案開頭，系統會略過它，避免把臨時檔或不完整檔案顯示到公開頁面。可保留 `.gitkeep`，它不會顯示於動態牆。
